# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
worktrees/
queue/

# Agent prompt files (generated per-session by launchers)
CLAUDE.md
AGENTS.md

# Experimental code/artifacts
dev/

# Autoresearch runtime outputs (untracked across experiments)
results.tsv
*.log
progress.png
.claude/

# Ignore results directories
results/


# Model checkpoints (large, rebuilt each experiment)
checkpoints/

# Raw data (large source files)
data/source/

# uv lock file (regenerate with: uv lock)
uv.lock
PLAN_TOMORROW.md
PROPOSAL_*.md

# Checkpoints (large binaries; runtime + backups, never committed)
checkpoints/
checkpoints.bak_*/
k_ablation_summary.txt
# Saved best-of-sweep weights — future home is a separate weights repo / LFS.
# Two ckpts briefly landed in history (c615660, removed by 5793057); this rule
# prevents accidental re-adds.
best_weights/
