[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "transform_emr"
version = "0.1"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["."]
include = ["transform_emr*"]